export interface Organization {
  id: string;
  name: string;
  description: string;
  category: string;
  tags: string[];
  country: string;
  website: string;
  image: string;
  verified: boolean;
}
